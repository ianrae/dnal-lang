type X enum {
  RED
  GREEN
  BLUE
} end


type Address struct { street X city string} end 
type Person struct { name string addr Address } end 
let a10 Address = { RED, 'Ottawa' }
let x Person = { 'abc', via street RED  }

